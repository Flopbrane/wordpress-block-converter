# DESIGN

## Markdown変換方針

Markdown変換は、記号をその場で置換するのではなく、以下の順番で処理する。

1. ファイルを行単位で読む。
2. 各行の先頭記号と状態を見て、まずブロック構造へ分ける。
   - `#` は数を数えて見出しブロックにする。
   - 三連バッククォート内はコードブロックとして扱い、中の `#`、`|`、`-` などはMarkdown記号として解析しない。
   - `-` / `*` が続く行は箇条書きブロックにまとめる。
   - `数字. ` または `数字) ` が続く行は番号付きリストブロックにまとめる。
   - 表、引用、画像、区切り線、ショートコードなども段落へ混ぜず、先にブロックとして確定する。
   - どのブロックにも当てはまらない行は段落ブロックに入れる。
3. ブロックが確定した後、そのブロックの本文だけにインライン装飾を適用する。
   - インラインコード
   - 太字
   - リンク
   - その他の本文向け装飾
4. 最後に、Markdown記号が不自然に残っていないか検査しやすい構造にする。

この方針の目的は、見出しが段落に混ざる事故、リストや表が段落へ巻き込まれる事故、コードブロック内の記号を誤解析する事故を減らすことである。

Markdown記号を最終段階で無条件削除することは避ける。本文として必要な記号まで消す可能性があるため、削除よりも検出・警告できる構造を優先する。
