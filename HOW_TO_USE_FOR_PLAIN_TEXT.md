# Rules for Plain Text

このファイルは、WordPressへ変換する前の平文に、少しだけ目印を付けるためのルールです。

普通の `.txt` は、空行で段落を分けるだけのシンプルな変換です。見出し、リスト、引用、コード、表、リンク、画像を指定したい場合は、ファイルを `.wp_txt` または `.wptxt` として保存してください。

## 基本方針

- `.txt`: 段落中心の平文として変換します。
- `.wp_txt`, `.wptxt`: 目印付きの平文として変換します。
- Markdownのバッククォート記法は使いません。
- HTMLを直接書くより、下の目印を使う方が初心者向けで安定します。

## 目印一覧

| 書き方 | 変換先 |
|---|---|
| `【見出し】` | h2見出し |
| `《小見出し》` | h3見出し |
| 空行 | 段落区切り |
| `・項目` | 箇条書き |
| `1. 項目` | 番号付きリスト |
| `> 引用文` | 引用 |
| `---` | 区切り線 |
| `[余白:50]` | 50pxの余白 |
| `[リンク:表示文字|https://example.com/]` | リンク |
| `[画像:https://example.com/image.jpg|代替テキスト]` | 画像 |
| `[コード]` から `[/コード]` | コードブロック |
| `[表]` から `[/表]` | 表 |

## 書き方の例

```text
【WordPressとは】

WordPressは、ホームページやブログを作るための仕組みです。

《できること》

・記事を書く
・画像を入れる
・表を作る

1. 下書きを作る
2. 変換する
3. WordPressへ貼り付ける

> これは引用文です。

詳しくは [リンク:公式サイト|https://example.com/] をご覧ください。

[画像:https://example.com/image.jpg|説明画像]

[コード]
<p>これはコード例として扱います。</p>
[/コード]

[表]
項目|説明
h2|大きな区切り
p|通常の本文
[/表]

---

[余白:50]
```

## 保存と変換

上のような目印を使う場合は、たとえば `sample.wp_txt` として保存します。

```powershell
python .\main.py .\sample.wp_txt .\sample_wordpress.html
```

目印を使わない普通の文章だけなら、`sample.txt` のままで大丈夫です。

```text
これは1つ目の段落です。

これは2つ目の段落です。
```

## 注意

`.wp_txt` はMarkdownではありません。シングルバッククォートや三連バッククォートは、Markdownファイルだけで使う記法です。

HTMLタグをそのまま書きたい場合でも、初心者向けには `[コード]`、`[表]`、`[リンク:...]`、`[画像:...]` のような目印を使う方針をおすすめします。
